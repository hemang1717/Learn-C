#include<stdio.h>
int main()
{
    char *num1="abc";
    char *num2="def";
    printf("%s %s",num1,num2);

    size_t len1=strlen(num1);
    // int len2=strlen(num2);
    // printf("%d %d",strlen(num1),strlen(2));

}